
import * as colors from "./colors";
export const text = {
  primary: colors.COLOR_TEXT_PRIMARY,
  secondary: colors.COLOR_TEXT_SECONDARY,
  disabled: colors.COLOR_TEXT_DISABLED,
  gold: colors.COLOR_GOLD_PRIMARY,
};
export const common = {
  black: colors.COLOR_COMMON_BLACK,
  contrast: colors.COLOR_COMMON_CONTRAST,
  white: colors.COLOR_COMMON_WHITE,
  gold: colors.COLOR_GOLD_PRIMARY,
};

export const primary = {
  main: colors.COLOR_PRIMARY,
};

export const secondary = {
  main: colors.COLOR_SECONDARY,
};

export const tertiary = {
  main: colors.COLOR_TERTIARY,
};

export const background = {
  default: colors.COLOR_BACKGROUND_DEFAULT,
};

export const error = {
  main: colors.COLOR_ERROR,
};

export const warning = {
  main: colors.COLOR_WARNING,
};

export const info = {
  main: colors.COLOR_INFO,
};

export const success = {
  main: colors.COLOR_SUCCESS,
};